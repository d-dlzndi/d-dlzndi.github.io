import PixelHome from "@/components/animation/pixelTransition/Page";
import SlideTransition from "@/components/animation/slideTransition";
import LandingPage from "./LandingPage";

export default function Index(params: any) {
  return <LandingPage {...params} />;
}

// <h2 className="w-full text-5xl text-center p-32">작업 중 . . . .</h2>
